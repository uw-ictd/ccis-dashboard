// urlPrefix is used for all assets on the dashboard. For example, if
// urlPrefix is '/dashboard' then the home page is something like
// odkxdemo.com/dashboard, and the export api calls use something like
// odkxdemo.com/dashboard/api/rawTable
module.exports = {
    URL_PREFIX: '/dashboard'
}
